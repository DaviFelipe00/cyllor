---
// Hero: Full-bleed photo, giant display title, elegant tagline
---
<section
  id="hero"
  class="relative min-h-screen flex flex-col overflow-hidden"
>
  <div class="absolute inset-0 z-0">
    <img
      src="https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?w=1920&q=85&auto=format&fit=crop"
      alt="Condomínio residencial moderno iluminado ao entardecer"
      class="w-full h-full object-cover object-center"
      loading="eager"
    />
    <!-- White elegant overlay gradient, reduced spread to reveal more image -->
    <div class="absolute inset-0" style="background: linear-gradient(to right, rgba(255,255,255,0.95) 0%, rgba(255,255,255,0.85) 25%, transparent 65%);" />
    <!-- Bottom fade to solid white to blend with the next section -->
    <div class="absolute inset-0" style="background: linear-gradient(to bottom, rgba(255,255,255,0.7) 0%, rgba(255,255,255,0) 20%, rgba(255,255,255,0) 80%, var(--bg) 100%);" />
  </div>

  <!-- Top divider line -->
  <div class="absolute top-0 left-0 right-0 h-px z-10" style="background: linear-gradient(90deg, transparent, rgba(37,99,235,0.15), transparent);"></div>

  <!-- Content layer -->
  <div class="relative z-10 flex flex-col justify-between min-h-screen px-6 sm:px-10 lg:px-14 pt-28 pb-14">

    <!-- Title + subtext stacked -->
    <div class="flex flex-col justify-center max-w-[1500px] mx-auto w-full gap-4 lg:gap-8 flex-1 mt-10 sm:mt-16">

      <!-- CYLLOR title & Tagline -->
      <div class="w-full">
        <div class="flex items-center gap-4 mb-2 lg:mb-4" data-reveal>
          <div class="w-10 h-px bg-blue-500/30"></div>
          <p class="font-mono text-[0.55rem] sm:text-[0.6rem] tracking-[0.28em] uppercase text-blue-500">Software para Condomínios</p>
        </div>
        
        <h1
          id="hero-title"
          class="font-display font-bold leading-[0.95] tracking-tighter select-none"
          style="font-size: clamp(3.5rem, 12vw, 8.5rem); color: var(--text); letter-spacing: -0.02em;"
          data-reveal
        >
          CYLLOR
        </h1>
      </div>

      <!-- Subtext stacked right below -->
      <div class="max-w-[420px]" data-reveal="left" data-delay="150">
        <h2 class="font-display font-light tracking-tighter text-3xl sm:text-4xl leading-tight mb-4" style="color: var(--text);">
          Operação inteligente<br/>
          <span class="font-bold text-blue-600">em perfeita harmonia.</span>
        </h2>
        <p class="font-body text-sm leading-relaxed mb-8" style="color: var(--text-dim);">
          Centralize reservas, comunicados e controle de acesso. A Cyllor simplifica a gestão e eleva a experiência dos moradores.
        </p>
        <a href="#contact" class="btn-gold-fill inline-flex" data-cursor-hover>
          Agendar demonstração
        </a>
      </div>
    </div>

    <!-- Bottom: scroll indicator -->
    <div id="hero-scroll-hint" class="flex items-center gap-4 max-w-7xl mx-auto w-full">
      <div class="flex flex-col items-center gap-2">
        <span class="font-mono text-[0.55rem] tracking-[0.3em] uppercase text-blue-500">SCROLL</span>
        <div class="w-px h-10 animate-float bg-blue-500/40"></div>
      </div>
    </div>

  </div>
</section>
