---
// Lifestyle: "Como funciona" — light cards with photo top
const items = [
  {
    title: 'Portaria mais ágil',
    desc: 'Autorizações de visitantes, entregas e acesso com histórico simples para equipe e moradores. Menos improviso, mais rastreabilidade.',
    tag: 'Controle de Acesso',
    img: 'https://images.unsplash.com/photo-1486325212027-8081e485255e?w=800&q=80&auto=format&fit=crop',
  },
  {
    title: 'Reservas sem atrito',
    desc: 'Salão, churrasqueira, piscina e demais áreas comuns organizadas em um calendário fácil de usar. Regras visíveis, conflitos eliminados.',
    tag: 'Áreas Comuns',
    img: 'https://images.unsplash.com/photo-1571896349842-33c89424de2d?w=800&q=80&auto=format&fit=crop',
  },
  {
    title: 'Comunicados e facilidades',
    desc: 'Avisos, documentos, assembleias e solicitações reunidos em uma experiência clara para síndico, administradora e morador.',
    tag: 'Comunicação',
    img: 'https://images.unsplash.com/photo-1497366216548-37526070297c?w=800&q=80&auto=format&fit=crop',
  },
];
---
<section id="lifestyle" class="relative py-24 sm:py-32 overflow-hidden" style="background: var(--bg-alt);">

  <div class="section-divider"></div>

  <div class="max-w-7xl mx-auto px-6 sm:px-10 lg:px-14">

    <!-- Header -->
    <div class="mb-14 sm:mb-20 flex flex-col sm:flex-row sm:items-end sm:justify-between gap-6">
      <div data-reveal>
        <span class="section-label mb-5 block">(Como funciona)</span>
        <h2 class="font-display font-semibold leading-tight" style="font-size: clamp(2.5rem, 5.5vw, 5rem); color: var(--text);">
          O fluxo do condomínio,<br/>
          <span class="font-normal" style="color: var(--blue);">simplificado.</span>
        </h2>
      </div>
      <p class="font-body text-sm leading-relaxed max-w-xs hidden sm:block" style="color: var(--text-dim);" data-reveal="right">
        Cada módulo foi desenhado para reduzir o retrabalho da gestão e melhorar a experiência de quem mora.
      </p>
    </div>

    <!-- Cards grid -->
    <div class="grid md:grid-cols-3 gap-5 sm:gap-6">
      {items.map((item, i) => (
        <article
          class="group flex flex-col gold-border rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-500"
          style="background: var(--surface);"
          data-reveal
          data-delay={i * 120}
          data-cursor-hover
        >
          <!-- Photo top -->
          <div class="img-zoom aspect-[4/3] overflow-hidden relative">
            <img
              src={item.img}
              alt={item.title}
              class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
            />
            <div class="absolute inset-0" style="background: linear-gradient(to top, rgba(15,23,42,0.8) 0%, transparent 60%);"></div>
            <!-- Tag overlay -->
            <div class="absolute bottom-4 left-4">
              <span class="font-mono text-[0.55rem] tracking-[0.22em] uppercase px-3 py-1.5 rounded-full" style="background: rgba(255,255,255,0.9); color: var(--blue);">
                {item.tag}
              </span>
            </div>
          </div>

          <!-- Text content -->
          <div class="p-6 sm:p-8 flex flex-col flex-1">
            <h3 class="font-display font-semibold text-2xl sm:text-3xl mb-4 transition-colors duration-300 group-hover:text-blue-600" style="color: var(--text);">
              {item.title}
            </h3>
            <p class="font-body text-sm leading-relaxed flex-1" style="color: var(--text-dim); line-height: 1.8;">
              {item.desc}
            </p>
            <!-- Blue accent line -->
            <div class="mt-6 h-px w-0 group-hover:w-full transition-all duration-500 bg-blue-500"></div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>
